export const config = {
  apiBaseUrl: "http://localhost:8080/",
};

export const localStorageKeys = {
  username: "username",
  firstName: "firstName",
  lastName: "lastName",
  jwtToken: "jwtToken",
  role: "role",
};

// http://localhost:3001/  for mock api
// http://localhost:8080/ for real api
